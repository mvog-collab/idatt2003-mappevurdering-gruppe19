package edu.games.engine.model;

/**
 * Represents the possible tokens (or colors) a player can choose in the game.
 * Each token corresponds to a unique visual identity on the board.
 */
public enum Token {
  BLUE,
  GREEN,
  YELLOW,
  RED,
  PURPLE
}
